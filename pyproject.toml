[project]
name = "smart-cv-analyzer"
version = "1.0.0"
description = "Сервис извлечения и структурирования данных из резюме"
requires-python = ">=3.12"

[tool.pytest.ini_options]
pythonpath = ["."]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
target-version = "py312"
line-length = 120
